# Fleet Dispatcher — geospatial endpoint environment.
# Connects as fleet_gis (owns the gis views; search_path = gis, fleet, public).
GIS_DATABASE_URL=postgresql://fleet_gis:fleet_gis@localhost:5432/fleet_dispatcher
GIS_PORT=5701
