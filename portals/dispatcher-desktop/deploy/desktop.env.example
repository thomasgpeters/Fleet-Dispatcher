# Fleet Dispatcher — Dispatcher Desktop environment.
# Copy to /etc/fleet-dispatcher/desktop.env (read by the systemd unit).

# Shared JSON:API endpoint (ApiLogicServer). The app reads this at startup.
FLEET_API_BASE_URL=http://localhost:5659/api
