import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.imagery.fleetdispatcher",
  appName: "Fleet Dispatcher",
  webDir: "dist",
};

export default config;
