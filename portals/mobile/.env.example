# Fleet Dispatcher mobile — environment
# Copy to .env (or .env.local) and point at your locally-deployed middleware.

# Base URL of the ApiLogicServer JSON:API.
VITE_API_BASE_URL=http://localhost:5659/api
