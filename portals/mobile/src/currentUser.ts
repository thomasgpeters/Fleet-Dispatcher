// The signed-in identity now comes from the auth context (see
// src/auth/AuthContext.tsx → useAuth(): user, driverId, equipmentId). What
// remains here is a non-identity reference constant.

// location_source id for a driver's phone push (matches database/seed_data.sql).
export const PHONE_PUSH_SOURCE_ID = 3;
